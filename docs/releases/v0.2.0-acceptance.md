# v0.2.0 发布产物验收

- 版本：`v0.2.0`
- 提交：`2ac75f11c3cb1c2cff40e17def872bc0efc46967`
- 验收日期：2026-07-26
- 测试环境：macOS 15.5（24F74），arm64

## 已通过

- GitHub Release 页面标记为 Latest，tag 和提交匹配。
- 从已登录 GitHub Release 页面重新下载 ZIP 和 `.sha256`，未使用本机发布目录代替。
- ZIP SHA-256：
  `b222a3556b91aa51258ef8768374ce861fcf6382d80faaa6c949b3771b3ca1d0`
- `.sha256` 文件 SHA-256：
  `3e57cb869ee67c3239e93a74cd28652010744cb75a7067b572910ec9a5cc1ac6`
- `shasum -a 256 -c` 通过。
- ZIP 必需结构、App 版本 `0.2.0`、build `1` 和单一 `arm64` 架构通过。
- `codesign --verify --deep --strict` 通过。
- App 大小为 2248 KiB，低于 10 MiB 上限。
- 浏览器下载的 ZIP 和解压 App 均带隔离属性。
- 对临时副本移除隔离后，进程连续运行 3 秒并正常响应终止信号。

## 阻断公开分发

- 无 GitHub 登录态的直接资产下载返回 `404`，登录后下载正常。这与私有仓库的行为一致。
- 如果目标是向任何用户公开分发，必须先让 Release 资产支持匿名下载，或选择独立的公开下载渠道，再重新执行匿名下载验收。

## 未执行

- 没有自动绕过 Gatekeeper，也没有点击“仍要打开”。当前版本为 ad-hoc 签名且未公证，`spctl` 未放行，符合 README 描述的预期边界。
- 菜单栏弹窗、关于、设置和退出的人工界面检查未执行。菜单栏-only App 在未打开窗口时没有向本机辅助控制接口暴露可操作窗口；该部分保留为两分钟人工检查。
- 登录启动、休眠/唤醒、不同供电状态、低电量模式和约 45 分钟轻量化门禁未执行，因为本轮明确排除耗时或需要特殊条件的检查。

## 结论

`v0.2.0` 的已登录下载、完整性、包结构、签名、架构、体积和启动存活检查通过。由于匿名下载不可用且人工界面检查尚未完成，本次结论为“有条件通过”，不能作为公开分发验收通过。
